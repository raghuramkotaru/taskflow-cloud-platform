# Template only. In a real cluster, supply these via a sealed secret or an
# external secrets operator; never commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secret
  namespace: taskflow
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: taskflow
  SPRING_DATASOURCE_PASSWORD: changeme
